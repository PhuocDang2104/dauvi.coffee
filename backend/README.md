# Backend

Thư mục dành cho backend tương lai. Frontend hiện dùng mock repositories; API dự kiến được mô tả tại `../docs/api-contracts.md`.

Chưa có backend, database, authentication, payment hoặc order service thật trong phiên bản này.
